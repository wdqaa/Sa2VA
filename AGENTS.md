# ChartGround-Edit 开发约定

- 新项目代码优先放在 `projects/chartground_edit/`，避免无必要修改上游 Sa2VA 核心代码。
- 修改前先阅读相关源码，引用真实文件、类和函数，不凭空推测行为。
- 每次只完成一个可验证的小阶段，并保留清晰的验收条件。
- 图像与 mask 必须使用一致的几何变换；任何裁剪、缩放、翻转都要同步。
- 在 32 个样本上无法稳定过拟合前，不运行完整训练。
- 不允许编造实验指标；缺失结果明确记为“未运行”或“待验证”。
- 未经确认，不下载大型模型、权重或数据集。
- 修改后运行相关测试并检查 `git diff`，不得覆盖无关的用户改动。
- 实验配置、结果、问题和结论及时记录到根目录 `EXPERIMENTS.md`。
